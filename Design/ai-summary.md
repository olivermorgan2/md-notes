# md-notes — AI summary

**Last regenerated:** 2026-05-04

This document is an AI-readable summary of md-notes. Sections are wrapped in `<!-- workflow-docs:start:... -->` / `<!-- workflow-docs:end:... -->` markers and are regenerated by the kit's `/workflow-docs` skill. Manual edits outside markers are preserved across re-runs.

<!-- workflow-docs:start:objectives -->
## Objectives

- Replace the "sticky notes and random `.txt` scratch files" workflow with a single CLI that writes dated markdown files into a user-owned folder and finds them again by half-remembered phrase.
- Success for v0.1 is the user no longer reaching for sticky notes and being able to recover an old note in seconds.

Concrete v0.1 success criteria:

1. Run `notes new "<title>"` and have a dated, frontmatter-bearing markdown file open in `$EDITOR` within seconds.
2. Run `notes find <half-remembered phrase>` against a directory of hundreds-to-thousands of notes and get the right one back, fast.
3. Run `notes ls` and see the notes touched today and yesterday at the top.
4. Stop reaching for sticky notes and ad-hoc `.txt` scratch files for short-term thoughts.
<!-- workflow-docs:end:objectives -->

<!-- workflow-docs:start:constraints -->
## Constraints (product principles)

1. Files on disk are the source of truth — no proprietary format, no database of record. Any external editor can open and edit a note.
2. The tool stays out of the way — no new mental model; file-on-disk reasoning is enough to be productive.
3. No services, no daemons, no network — single binary or `pipx` install; no Docker, no background processes, no telemetry.
4. Sync, encryption, and collaboration are someone else's job — the user picks iCloud / git / Syncthing (or none).
5. Fast at thousands of notes — search latency is a first-class success criterion.
<!-- workflow-docs:end:constraints -->

<!-- workflow-docs:start:current-status -->
## Current status

- **v0.1.0 released** on 2026-05-04: <https://github.com/olivermorgan2/md-notes/releases/tag/v0.1.0>.
- Foundational ADRs 001–005 accepted on 2026-05-02.
- Issues #1 (Go scaffold + CI) and #2 (config loader) shipped via PRs #12 and #13.
- Active milestone: **M1 — md-notes v0.1 — full MVP delivery**.
- Remaining v0.1 issues: #3 (notes new), #4 (notes find), #5 (notes ls), #6 (test coverage), #7 (latency smoke test), #8 (one-week dry-run), #9 (cross-platform packaging), #10 (README), #11 (CHANGELOG + tag).
<!-- workflow-docs:end:current-status -->

<!-- workflow-docs:start:key-decisions -->
## Key decisions

- **ADR-005** (accepted 2026-05-02): in-process scan in Go for full-text search; preserves single-binary distribution and the no-daemon constraint.
- **ADR-004** (accepted 2026-05-02): minimal YAML frontmatter (`title`, `date`, `tags: []`) — stable shape so v0.2 tagging adds keys without migrating older notes.
- **ADR-003** (accepted 2026-05-02): filenames as `YYYY-MM-DD-slug.md` with numeric collision suffix; sorts naturally and prevents same-title-same-day overwrites.
- **ADR-002** (accepted 2026-05-02): configuration via `--dir` flag > `MD_NOTES_DIR` env > `~/.config/md-notes/config.toml` > `$HOME/notes` default.
- **ADR-001** (accepted 2026-05-02): Go as the implementation language — matches the single-binary distribution constraint.
<!-- workflow-docs:end:key-decisions -->
